
  # TFW Organization Chart

  This is a code bundle for TFW Organization Chart. The original project is available at https://www.figma.com/design/Lok6IgClr02RHpn6dCsgUm/TFW-Organization-Chart.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  